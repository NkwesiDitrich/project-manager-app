# Copy this file to .env and fill in your values. Do not commit .env.
# Used at build time: set this before running npm run build in production.

# Backend API base URL (must end with /api-v1). In dev: http://localhost:3001/api-v1
VITE_API_URL=http://localhost:3001/api-v1
